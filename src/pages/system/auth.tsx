import * as React from 'react';
import { } from 'antd';
interface authProps { }

const auth = function (props: authProps) {
  return <div />;
};
// auth.title = '权限管理';
// auth.icon = 'safetyCertificate';
// auth.sort = 5;

export default auth;
