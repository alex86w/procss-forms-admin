import Title from './title';
import { Divider } from 'antd';
import Descirption from './description';

const ContentObj: any = {
  SingTitle: [Title, Divider, Descirption],
};

export default ContentObj;
