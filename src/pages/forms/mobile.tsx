import React from 'react'
import { FormItems } from '@/services/interface/forms.interface'

const Mobile = (props: any) => {
    console.log('mobile11', props)
    return (
        <div>
            mobile
        </div>
    )
}

export default Mobile

